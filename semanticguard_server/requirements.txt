fastapi>=0.111.0
uvicorn[standard]>=0.29.0
peft>=0.10.0
transformers>=4.40.0
torch>=2.2.0
# Install unsloth separately — see bootstrap script for correct CUDA variant:
# pip install "unsloth[colab-new] @ git+https://github.com/unslothai/unsloth.git"
# or: pip install unsloth
accelerate>=0.29.0
bitsandbytes>=0.43.0
